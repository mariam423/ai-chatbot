# Project knowledge

This file gives Freebuff context about your project: goals, commands, conventions, and gotchas.

## Quickstart

- Setup: `npm install` (postinstall generates the Prisma client — no `DATABASE_URL` needed for install); copy `.env.example` → `.env.local`, set `OPENROUTER_API_KEY` (OpenRouter — OpenAI-compatible, free models available; `OPENAI_API_KEY` also works for other endpoints) and a Postgres `DATABASE_URL` (local Postgres or Neon free tier); then `npx prisma migrate dev` to apply the schema
- Dev: `npm run dev` — Next.js app at localhost:3000 (chat UI at `/`, streaming LLM proxy at `/api/chat`)
- Test: `npm test` (vitest) — validates agents, skills, and the SSE stream parser; `npm run check` = typecheck + tests

## Architecture

- Key directories:
  - `app/` — Next.js App Router: `page.tsx` (chat UI), `layout.tsx`, `api/chat/route.ts` (server-side streaming proxy to an OpenAI-compatible LLM API; credentials never reach the client)
  - `components/` — client chat UI: `chat-app.tsx` (shell: sidebar + drawer + dark mode + session switching), `chat.tsx` (streaming/stop/retry/regenerate/clear + Framer Motion + empty-state capability chips fetched from `/api/skills`; the thread is a branch-aware `ThreadState` — editing a past prompt forks a new branch and regenerates, with a version switcher bar to toggle between branches — and it wires the voice mic + export menu into the composer), `skill-picker.tsx` (per-session skill toggle dropdown in the header — persisted via `updateSessionSkills`/`getSessionSkills`, sent as `enabledSkills` in chat requests), `sidebar.tsx` (desktop session list + mobile overlay drawer — focus trap, Escape, inert when closed — with per-session rename/delete actions), `markdown.tsx` (LLM replies with highlighted code blocks + copy buttons; a custom `urlTransform` preserves SVG data URLs so `diagram_render` images render), `diagram-card.tsx` (SVG diagram card — rendered via `<img>` (script-safe), with a spring-animated loading skeleton, Copy-SVG and Download buttons, and a full-screen pan/zoom viewer), `diagram-viewer.tsx` (glassmorphic full-screen diagram viewer — wheel/button/double-click zoom, drag-pan clamping, Escape/backdrop close with focus restore), `message-bubble.tsx` (user bubbles expose an **Edit prompt** action → inline editor that forks a new branch), `chat-export.tsx` (Export chat menu: downloads the active branch as Markdown/JSON blobs and renders a styled transcript into a hidden iframe for native **Print → PDF** — no PDF dependency), `audio-input.tsx` (composer mic: Web Speech `SpeechRecognition` where available, else a MediaRecorder fallback that records and POSTs to `/api/transcribe` for server-side transcription — `pickVoiceEngine` decides; pulsing recording indicator + animated equalizer while listening/transcribing, transient error pill on failure), `streaming-skeleton.tsx`
  - Design system: **Cyber Emerald & Obsidian Gold** — `app/globals.css` holds the full token set (obsidian/emerald dark + warm-paper light, `--gold` reserved for the brand mark + pinned states, emerald focus rings, dark emerald-tinted code theme); Inter (body) + Space Grotesk (display) via `next/font` in `app/layout.tsx`. Components use `var(--...)` tokens or Tailwind `emerald-*` classes (no raw cyan remains); the sidebar's active state is a Framer `layoutId` spring that slides between sessions. The `e2e/visual.spec.ts` snapshots encode the palette — regenerate with `--update-snapshots` after visual changes.
  - `lib/svg-data-url.ts` — browser-safe helpers for the diagram card: `isSvgDataUrl`, `decodeSvgDataUrl` (base64 or percent-encoded → raw SVG markup, validated to start with `<svg`), `svgFilename` (alt-derived download filename)
  - `lib/export-chat.ts` — Node-testable pure chat-export helpers: `chatToMarkdown`, `chatToJson`, `exportFileName` (used by `chat-export.tsx`); Markdown/JSON/PDF exports all carry each assistant reply's served model (`*via <model>*` / `model`+`modelOverridden` fields)
  - `lib/storage.ts` — versioned callback thread; payload v3 stores a `ThreadState` (`{ version: 3, branches, active }`, messages carrying optional `model`/`modelOverridden` so branch history shows which model served each reply) so editing a past prompt can fork a new branch while the previous branch stays toggleable; `loadThread`/`saveThread` remain active-branch conveniences and v2/v1/legacy payloads auto-migrate
  - `app/api/skills/route.ts` — GET endpoint exposing the skill catalog (instructions + tool JSON-schemas) and the effective active set; an `enabledSkills` query param mirrors the per-session override sent with `/api/chat`
  - `lib/sse.ts` — SSE parsing helpers (extractSSEEvents, deltaText, readSSEStream)
  - `lib/security.ts` — shared guardrails: `checkCsrf` (Origin/Referer guard), `sanitizeInput` (control-char strip — preserves `\t\n\r` so multi-line markdown/code blocks survive persistence), `requireSession` (NextAuth guard, lazily imported so the module is vitest-safe), and `guardRoute` — a one-call composition of CSRF + optional session + rate limit (user- or IP-scoped, env-var or literal limit, with `defaultLimit` fallback) returning a short-circuit response or the user id. Denial events (CSRF blocks, 401s, 429 trips, auth throttles) are emitted through `lib/audit.ts` (structured JSON security log).
  - `lib/audit.ts` — structured security audit logging (OWASP A09): one-line JSON events (`ts`, `event`, `level`, details) via `logSecurityEvent`. `warn` events (denials) log always outside NODE_ENV=test; `info` events (e.g. successful logins) are opt-in via `SECURITY_AUDIT_LOG=true`. Callers pass only ids/IPs/statuses — never bodies, headers, or secrets. Wired into `guardRoute`, `checkAuthRateLimit`/`checkLoginRateLimit`, the credentials `authorize`, `saveChatMessages` ownership violations, and the Stripe webhook signature failure path.
  - `lib/roles.ts` — shared `FREE`/`PRO`/`ADMIN` role schema and least-privilege normalization; Postgres persists the value as validated text.
  - `lib/ssrf.ts` — SSRF guard (OWASP A10): `assertSafeUrl` rejects non-http(s) schemes, private/loopback/link-local/reserved IPs (IPv4 + IPv6 incl. IPv4-mapped), unresolvable hostnames, and hostnames where ANY resolved address is private (DNS-rebinding defense). Wired into web search (`WEB_SEARCH_URL`), the MCP client (`MCP_SERVERS_JSON` urls), `diagram_render` (`DIAGRAM_RENDER_URL`), and `weather_lookup` (`WEATHER_API_URL`). Deliberately NOT applied to the LLM base URL so self-hosted local models keep working.
  - `lib/field-encryption.ts` — data-at-rest encryption for sensitive preference fields: `encryptField`/`decryptField` wrap values in an AES-256-GCM `v1:<iv>:<tag>:<ct>` envelope keyed by the `ENCRYPTION_KEY` env var (sha256-derived to 32 bytes). No key → plaintext passthrough with a one-time warning (local dev); legacy plaintext rows decrypt as-is; an encrypted row that fails GCM verification (rotated key, tampering, missing key) degrades to `''` and emits a structured `decryption_failed` audit event — never ciphertext, never a throw. Applied to `UserPreference.apiKey` and `UserPreference.googleServiceAccountKey` in `app/actions.ts` (encrypt on write, decrypt in `getUserPreferences`/`testGoogleCalendarConnection`) and `lib/skills/credentials.ts` (`getUserSkillContext` decrypts before parsing). Every API route's guard config lives in the exported `ROUTE_GUARDS` map (bucket namespace, scope, session, CSRF policy, limit) — the single reviewable source of truth, applied as `guardRoute(request, ROUTE_GUARDS.<route>)`: `/api/chat` (session + user scope), `/api/transcribe`, `/api/upload`, `/api/citation` (IP), `/api/analytics` (CSRF only), `/api/skills` (IP, no CSRF — GET), `/api/webhooks/stripe` (per-IP flood brake with a generous cap; signature verification remains the real auth), and `/api/health/queue` (session, no CSRF — GET; the ADMIN role is enforced inside the route). Re-exports `rateLimit`/`rateLimitResponse` from `lib/rate-limit.ts`. Also hosts the auth counterpart to `ROUTE_GUARDS`: the `AUTH_GUARDS` map (register 5/min, login 20/min per-IP + 10/min per-account, reset-request 5/min, reset-complete 10/min — all per-IP buckets through the shared store), `clientIpFromHeaders` (server actions have no `Request` — derives the same IP from `next/headers`), `checkAuthRateLimit` (server-action throttle returning the `{ ok: false; error }` shape), and `checkLoginRateLimit` (per-IP + per-account, runs before bcrypt in the credentials `authorize`)
  - `lib/rate-limit.ts` — shared rate-limit store: fixed window (slides forward on expiry) via a narrow `RateLimitStore` interface. Default is an in-memory `MemoryRateLimitStore`; when `REDIS_URL` is set a `RedisRateLimitStore` (ioredis, atomic Lua INCR+PEXPIRE so multi-instance buckets are race-free) shares counters across processes and survives restarts. A Redis outage degrades to memory (fail-fast client: lazyConnect, no reconnect strategy, error listener) so requests are never blocked by the limiter's infra
  - `lib/billing/` — SaaS tiers + Stripe: `plans.ts` (free/pro tiers, `dailyChatRequests` limits, `isOverDailyLimit`), `stripe.ts` (SDK-free Stripe REST client with timed checkout/portal requests, HTTPS response validation, and `verifyStripeWebhookSignature` with 5-min skew tolerance; env-gated, degrades to `notConfigured`), `usage.ts` (`checkAndRecordUsage` — per-day plan cap enforced before any LLM call). Webhook at `app/api/webhooks/stripe/route.ts` (signature-verified; upgrades/downgrades `users.plan` and `users.role`); authenticated, rate-limited checkout/portal server actions `upgradeToPro`/`openBillingPortal`/`getBillingStatus`; settings shows a Plan & Billing section
  - `lib/analytics.ts` — env-gated event tracking (PostHog capture via server-side fetch when `POSTHOG_API_KEY` set, or `ANALYTICS_DEBUG` console logging; complete no-op by default). Client bridge `app/api/analytics/route.ts` + `lib/use-analytics.ts` hook (pageview + `chat_message_sent`/`chat_session_created`/`billing_upgrade_clicked` events)
  - `lib/seo.ts` — static WebApplication JSON-LD + site metadata; root layout exports OpenGraph/Twitter metadata and renders the JSON-LD script; settings page sets a dynamic `document.title`
  - `lib/skills/registry.ts` — browser-safe skill & tool registry (importable from client components): 8 skill domains with per-skill system instructions, Zod schemas, tool metadata (JSON-schema parameters via `z.toJSONSchema`), `SKILLS_ENABLED` activation, and the client catalog (`getSkillCatalog`) — all activation helpers accept an explicit per-session skill-id list (`getActiveSkills`/`getSkillSystemInstructions`/`normalizeSkillIds`)
  - `lib/skills/tools.ts` — **server-only** skill tool executors (imports `node:crypto` and `lib/ssrf.ts`; never import from client code): `SKILL_TOOLS`, `executeSkillTool`, `listSkillTools`, `toOpenAISkillTools`. Executors call real providers when configured — `diagram_render` → Kroki-compatible `DIAGRAM_RENDER_URL` (default kroki.io, `ascii`→svgbob), `schedule_block` → Google Calendar service account (hand-rolled RS256 JWT, `GOOGLE_CALENDAR_ID`/`GOOGLE_SERVICE_ACCOUNT_EMAIL`/`GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` or per-user context) — and degrade to clearly marked local fallbacks (text preview / local block) when unset or the provider fails. Executors accept a `SkillToolContext` (`googleCalendar` credentials); `resolveGoogleCredentials` prefers the per-user context over env vars
  - `lib/skills/credentials.ts` — **server-only** `getUserSkillContext(userId)`: loads a user's pasted Google service-account key + calendar id from `UserPreference` into a `SkillToolContext` (empty when unauthenticated, unset, or the stored key won't parse, so executors fall back to env → mock)
  - `ChatSession.enabledSkills` (nullable) — per-session skill override persisted on `chat_sessions`; null = defaults, empty string = none enabled
  - `lib/db.ts` — PrismaClient singleton over Postgres (`@prisma/adapter-pg`; pool tuning via `lib/db-config.ts`); `prisma/schema.prisma` — ChatSession (optional user `title`) + ChatMessage + UserPreference (profile, presets, Google Calendar service-account key/calendar id for `schedule_block`, plus model tuning: `preferredModel` UI key, `temperature` 0–1, `maxCompletionTokens`); `app/actions.ts` — Server Actions for per-session persistence (FR-11) + `listChatSessions` (search + paginated sidebar list, each summary carrying the last assistant reply's model as `lastModel` for sidebar/header provenance) / `renameChatSession`, plus `updateUserPreferences` (validates a pasted service-account key JSON before saving; Zod-validates preferred model + tuning bounds) and `testGoogleCalendarConnection` (token exchange + calendar access check)
  - `.agents/` — custom Codebuff agents: one file per agent (`<id>.ts`), types in `.agents/types/`, skills in `.agents/skills/<name>/SKILL.md`
  - `tests/` — vitest suites: agent conventions, skill frontmatter, skill references, SSE parser
  - `PRD.md` — product requirements for the chatbot
  - `codebase-overview.md` — architecture map of what exists (components, data flow, conventions, tradeoffs, open questions); consult it before planning or modifying code, and update it when the architecture changes significantly
- Data flow: chat client POSTs history to `/api/chat`; the route forwards to the configured LLM API with `stream: true` and pipes the SSE stream back; the client parses deltas via `lib/sse.ts` and renders incrementally

## Publishing agents

- Agents in `.agents/` are **local** to this repo. Publishing makes an agent available in the Codebuff **agent store**, where it is addressed by its fully qualified id: `publisher/id@version` (e.g. `codebuff/file-picker@0.0.1`).
- To publish, the definition must include:
  - `publisher` — your publisher ID. Required for publishing (see `agent-definition.ts`).
  - `version` — version string. Optional; defaults to `0.0.1` and is bumped automatically on each publish.
- Referencing agents:
  - In `spawnableAgents`, use the short id for repo-local agents (e.g. `'code-reviewer'`) and the fully qualified `'publisher/id@version'` for store agents — publisher and version are required for store references.
  - `lookup_agent_info` accepts both formats (short local or full published).
- Publishing is done from the Codebuff CLI; `/init` is the documented first step for building custom agents. The exact publish command isn't in the public docs — confirm it in the Codebuff CLI help, docs, or Discord before publishing.

## Agents

- `project-planner` (`.agents/project-planner.ts`) — turns a goal into a concrete, executable plan. Loads the `planner-pro-max` skill workflow (outcome → phases → tasks → dependencies → range estimates → risks) via the skill tool, pulling in `architecture-designer` for architecture-heavy goals (new systems, restructuring, hard perf/scale/security constraints) and `product-manager-pro` when the goal or metrics are unclear. Spawns the `requirements-writer` agent for requirement-heavy goals (acceptance criteria become task done-criteria) and one `phase-planner` sub-agent per phase (via spawn_agents, in parallel) to detail tasks/estimates/risks, then folds the results into the final plan. Grounds estimates in the actual repo (read-only), asks blocking questions via ask_user, and returns a structured plan: outcome, phases (tasks with ids, dependsOn, estimates, done-criteria), risks with mitigations, open questions. Spawnable by other agents; model `anthropic/claude-sonnet-4.5` with high reasoning effort.
- `phase-planner` (`.agents/phase-planner.ts`) — sub-agent that details ONE phase of a larger plan: task decomposition with done-criteria, intra-phase dependencies, range estimates, and phase-level risks. Spawned by project-planner; read-only, never asks the user (parent owns interaction); returns a structured phase plan.
- `requirements-writer` (`.agents/requirements-writer.ts`) — writes atomic, testable functional/non-functional requirements with Given/When/Then acceptance criteria (embodies the `requirements-engineer` skill workflow). Read-only; returns structured requirements (id, category, statement, acceptance criteria, source) plus open questions. Spawnable by the project-planner for requirement-writing subtasks.
- `codebase-overview` (`.agents/codebase-overview.ts`) — maps the repo using the `architecture-designer` skill in documentation mode (describes the architecture as it exists): components with responsibilities/locations/contracts/dependencies, data flow, observed conventions, and unverified gaps. Read-only; returns a structured map. Useful before planning, reviewing, or modifying code.
- `code-reviewer` (`.agents/code-reviewer.ts`) — read-only reviewer of code/diffs for correctness, security, performance, and maintainability; returns structured findings (severity/file/line/issue/suggestion) plus an approve/request-changes verdict.

## Skills

Reusable skill instructions live in `.agents/skills/<skill-name>/SKILL.md` — each is a markdown file with YAML frontmatter (name, description, version) that the skill tool auto-loads by name. Current skills:

- `product-manager-pro` — Senior PM: turn vague ideas into problem statements, goals, success metrics, prioritized scope, and a concise PRD.
- `requirements-engineer` — Write atomic, testable functional/non-functional requirements with Given/When/Then acceptance criteria.
- `planner-pro-max` — Break goals into phases, tasks, dependencies, estimates, risks, and a trackable execution checklist.
- `architecture-designer` — Design pragmatic system/component architecture: boundaries, data flow, tradeoffs, ADRs — grounded in requirements and the existing codebase.
- `test-engineer` — Design and write effective automated tests: behavior-first, edge-case coverage, fast and reliable suites matching project conventions.
- `react-best-practices` — React component guidance (effects as escape hatches, refs, composition).
- `vercel-react-best-practices` — Vercel's React/Next.js performance rules (waterfalls, bundle size, localStorage schema versioning).
- `zod-schema-validation` — Zod schema validation + type inference; validate at system boundaries (API, forms, external storage).
- `prisma-database-setup` / `prisma-cli` / `prisma-client-api` — official Prisma skills (provider setup, CLI commands, client API).
- `server-actions` / `nextjs-server-actions` — Next.js Server Actions patterns (form handling, validation, revalidation).
- `prompt-optimizer` / `arize-prompt-optimization` — prompt optimization with evals and trace data.
- `session-compression` — conversation context compression (token-budget FIFO, summarization thresholds); user-invocable only.
- `compression` — JS payload compression (Gzip/Brotli) — not relevant to LLM context.
- `accessibility` — web accessibility (WCAG 2.2): focus, contrast, keyboard, reduced motion.
- `assistant-ui` — AI chat UI patterns (message lists, streaming states).
- `loading-states-and-perceived-performance` — skeleton/spinner loading patterns.
- `micro-interaction-spec` — micro-interaction specifications (states, motion, a11y).

Invoke one when the task calls for its role (product definition, requirements writing, or planning). New skills follow the same `<skill-name>/SKILL.md` layout; the directory name must match the `name` in the frontmatter.

## Tests

`npm test` (vitest) validates every agent and skill in the repo; `npm run check` runs typecheck + tests. The suites auto-discover files — no test needs updating when you add an agent or skill, only when you add a new convention.

- `tests/agent-definitions.test.ts` — validates every `.agents/*.ts` definition at runtime (the checks TypeScript can't express): at least one agent exists; id matches `^[a-z0-9-]+$`, is unique, and matches its filename; `displayName`/`model` and at least one prompt field are present; `toolNames` is an array of non-empty strings; `outputSchema` requires `outputMode: 'structured_output'` (and vice versa); `reasoningOptions` provides `max_tokens` or `effort`; spawnableAgents entries exist, have spawnerPrompts, and are instructed in the parent's prompts.
- `tests/skill-frontmatter.test.ts` — parses each `.agents/skills/*/SKILL.md` frontmatter and checks: `name` is present, `^[a-z0-9-]+$`, unique, and matches its directory; `description` is present and action-oriented; `version` is semver (`X.Y.Z`); `invoked_by` is one of `user | agent | both`; `user_invocable` is a boolean; `source` is `builtin | user-custom-rule`.
- `tests/skill-references.test.ts` — cross-checks agents against skills: every backtick-quoted kebab-case name in an agent's prompts must resolve to a real skill (tokens naming a tool in the agent's `toolNames` or a local agent id are excluded); any agent with the `skill` tool must reference at least one skill; agent ids and skill names must not overlap; every skill is referenced or user-invocable.
- `tests/sse.test.ts` — unit tests for `lib/sse.ts` (event extraction, delta parsing, streaming with abort).
- `tests/api-chat.test.ts` — route contract tests for `app/api/chat` (key guard, zod request-body validation with structured issues, 400/502/429 handling, system prompt + Authorization header, streaming passthrough) with mocked fetch.
- `tests/security.test.ts` — unit tests for `lib/security.ts` + `lib/rate-limit.ts`: rate-limit window/cap/retry-after, per-key isolation, `rateLimitResponse` 429 shape, `clientIp`/`clientIpFromHeaders` fallbacks, CSRF same-origin/cross-site/no-origin, `sanitizeInput` control-char strip + length cap, `guardRoute` (CSRF short-circuit, session passthrough, IP/user bucket scoping incl. a mocked-NextAuth signed-in user on `chat:user:<id>`, env-var limits + `defaultLimit` fallback, no-limit path), `ROUTE_GUARDS` (key/name consistency, exact route set, pinned limits/scopes/CSRF policy), `AUTH_GUARDS` (exact key set + pinned per-window caps; `checkAuthRateLimit` allows-up-to-cap-then-blocks with per-IP isolation; `checkLoginRateLimit` caps by IP and independently by account), and the rate-limit store (memory-vs-Redis selection by `REDIS_URL`, Lua INCR+PTTL mapping with a fake client, Redis-outage fallback to memory, expired-window reset).
- `tests/auth-security.test.ts` — behavior tests for the auth throttles through the real server actions (in-memory Prisma mock + mocked `next/headers`): `registerUser` allows 5 per IP then blocks before any bcrypt work (hash-spy asserts no hashing on the throttled attempt), a rotated IP gets a fresh bucket, `requestPasswordReset` allows 5 per IP without writing tokens, and `resetPassword` allows 10 per IP before the throttle.
- `tests/ssrf.test.ts` — unit tests for `lib/ssrf.ts` (mocked `node:dns/promises`): IPv4/IPv6 range blocking (loopback, private, link-local, CGNAT, ULA, IPv4-mapped), public-IP allowance, protocol whitelist, IP-literal short-circuits, hostname resolution with any-private-address rejection (DNS rebinding), unresolvable-host rejection.
- `tests/field-encryption.test.ts` — unit tests for `lib/field-encryption.ts`: round-trip envelope, random-IV uniqueness, no-key plaintext passthrough, legacy-row passthrough, encrypted-row-without-key → `''`, rotated-key auth-tag failure → `''`, tampered ciphertext → `''`. `tests/actions.test.ts` pins the wiring: with `ENCRYPTION_KEY` set the DB stores only `v1:` envelopes (no plaintext) while reads return plaintext, legacy rows are readable, and a rotated key degrades reads to empty + the calendar test to 'no credentials'. `tests/credentials.test.ts` covers the encrypted-stored-key → context and wrong-key → empty-context paths.
- `tests/audit.test.ts` — unit tests for `lib/audit.ts`: no-op under NODE_ENV=test, structured JSON warn shape in production, `SECURITY_AUDIT_LOG` gating for info events, and the `guardRoute`-blocked → `rate_limited` event wiring.
- `tests/billing.test.ts` — unit tests for `lib/billing/plans.ts` (plan parsing/limits/`isOverDailyLimit`) and `lib/billing/stripe.ts` (checkout/portal fallback contracts plus valid/tampered/stale/rotating-secret webhook signatures).
- `tests/analytics.test.ts` — unit tests for `lib/analytics.ts`: no-op without config, PostHog capture payload shape, provider-error swallow, and `ANALYTICS_DEBUG` console logging.
- `tests/webhook-stripe.test.ts` — route tests for `/api/webhooks/stripe` with mocked prisma: 501 unconfigured, 401 bad signature, pro upgrade on checkout completed, free downgrade on subscription deleted, unhandled events acknowledged without DB writes.
- `tests/prd-checklist.test.ts` — PRD traceability checklist: one row per FR/NFR from PRD.md with verification method and status; fails if PRD requirements are missing, renamed, or added without a row. Also asserts PRD.md names the configured provider (OpenRouter) and references `OPENROUTER_API_KEY`.
- `tests/storage.test.ts` — unit tests for `lib/storage.ts` (versioned localStorage thread persistence: v2 branched round-trip + active-branch selection, corrupt/invalid payloads → empty thread, v1 + legacy-format migration to a single branch with write-back, SSR guard, clear).
- `tests/export-chat.test.ts` — unit tests for `lib/export-chat.ts` (Markdown role sections + escaping + `*via <model>*` provenance line, JSON metadata + role/content messages with optional model fields, filename sanitization/fallback/truncation).
- `tests/api-transcribe.test.ts` — contract tests for `app/api/transcribe` (key guard, missing/oversized file, provider forwarding + model/language params, upstream error passthrough, unreachable/empty-transcript handling) with mocked fetch.
- `tests/voice-engine.test.ts` — unit tests for `pickVoiceEngine` (Web Speech preferred, MediaRecorder fallback, none available → null).
- `tests/actions.test.ts` — integration tests for the FR-11 Server Actions against an in-memory Prisma mock (persist + load in order, idempotent re-save, in-place update, validation rejects, create/clear session), plus user-preferences coverage (round-trip incl. Google Calendar key, malformed-key rejection, clear, and `testGoogleCalendarConnection` success/403/not-configured paths with a mocked token exchange).
- `tests/context.test.ts` — unit tests for `lib/context.ts` (history truncation to last N, token-budget FIFO, newest message always kept).
- `tests/credentials.test.ts` — unit tests for `getUserSkillContext` (unauthenticated / no prefs / valid key → context, invalid stored key, DB failure degradation) with the Prisma client mocked.
- `tests/svg-data-url.test.ts` — unit tests for `lib/svg-data-url.ts` (base64 + percent-encoded decoding, malformed/non-SVG rejection, alt-derived filenames).
- `tests/skill-registry.test.ts` — unit + integration tests for `lib/skills/registry.ts` (8-domain metadata, Zod→OpenAI schema binding, executor happy paths, schema/JSON/unknown-tool fallbacks, `SKILLS_ENABLED` activation, intent detection, agent-loop binding, client catalog, and route skill injection).
- `tests/api-skills.test.ts` — contract tests for `app/api/skills` (catalog shape, `enabledSkills` override narrowing, empty override, unknown-id filtering, `SKILLS_ENABLED` fallback).
- `tests/live-providers.test.ts` — **manual** live integration tests for the skill tool providers, skipped unless `RUN_LIVE_PROVIDER_TESTS=true` (so CI stays green without credentials/network): renders a diagram through the real Kroki service, and creates+deletes a test event in a sandbox Google Calendar (Google test additionally skips without `GOOGLE_*` env).
- `e2e/diagram.spec.ts` — Playwright e2e for the SVG diagram card: a streamed reply embedding an SVG data URL renders the card, Copy puts the exact SVG markup on the clipboard (clipboard permissions via `test.use`), Download saves `diagram.svg` (download event), and a non-SVG image renders inline without diagram controls.
- `e2e/chat.spec.ts` — Playwright e2e for the chat flow (mocked /api/chat, no API key needed): empty state, streamed reply, typing indicator + stop, error + retry, persistence (reload restore + versioned `{ version, branches, active }` payload shape, asserted against `lib/storage` constants), clear, empty-input guard. Run with `npm run test:e2e` (config in `playwright.config.ts`; runs the production build). Thread-text assertions are scoped to `main` because the sidebar lists conversation titles too. Note: the Next.js route announcer also has `role=alert`, so e2e targets the error via `data-testid="chat-error"`.
- `e2e/branch-export.spec.ts` — Playwright e2e for forking + export (mocked echo `/api/chat`): editing a past prompt forks a new branch with a regenerated reply, the version switcher shows both branches (active V2), toggling V1↔V2 restores each thread's own reply (context preserved), and the export menu is disabled on an empty thread but downloads `.md` and `.json` files once a conversation exists.
- `e2e/long-reply-export.spec.ts` — Playwright e2e pinning the client contract around long streamed replies: every provider request carries the small 200-token `max_tokens` cap (see `lib/llm-config.ts`) and the client must still render/export the FULL reply — a streamed reply of ~7,600 tokens is rendered in full, exported as Markdown with the content past the 200-token boundary intact (marker-index assertions), JSON export carries the tail, and the full reply survives a reload round-trip — any client-side truncation (verified by truncating the mock at the cap) fails it.
- `e2e/proxy-stream.spec.ts` — the one spec that streams a full chat through the **real** `/api/chat` proxy (every other spec mocks the route in the browser; `rate-limit.spec` hits the real route only with invalid `{}` bodies). A local mock upstream (`e2e/mock-llm-server.mjs`, wired via `OPENROUTER_BASE_URL`/`OPENROUTER_API_KEY` overrides in the Playwright webServer env, which beat `.env`/`.env.local`) records every chat-completions body and exposes it at `/__last-request`; the spec asserts the server sends the conservative 200 `max_tokens` cap on OpenRouter defaults (the 402 pre-authorization fix — a tiny explicit cap keeps the pre-auth cost near zero; omitting it makes OpenRouter pre-authorize against the model max and reject low-credit keys, verified live) and that a ~38k-char reply streams back through the proxy with markers past the 200-token cap intact (SSE decoded via `lib/sse`). It keys its rate bucket to a TEST-NET-3 IP via `x-forwarded-for` so it never collides with `rate-limit.spec`'s shared `chat:ip` bucket, and skips (rather than fails) when the app server is a reused instance not wired to the mock.
- `e2e/sidebar.spec.ts` — Playwright e2e for Phase 3 UI: sidebar session list + New Chat reset, dark mode toggle persistence, regenerate (replaces the last assistant reply), markdown code blocks (language badge + one-click copy; grants clipboard permissions via `test.use`), the mobile drawer (open → list → New Chat reset → session switch → Escape close), and session rename/delete (row menu → inline rename input; two-step delete confirm that resets the active thread) plus debounced search (title/content, case-insensitive) and Show-more pagination (page of 20; the Show-more test waits for the list to load before checking, since `count()` doesn't auto-wait).
- `e2e/a11y.spec.ts` — Playwright e2e accessibility checks (NFR-2): keyboard walk through the sidebar with visible focus rings (asserted against the real `:focus-visible` outline, so it uses genuine Tab presses), `aria-current` on the active session (cleared by New Chat, re-marked on select, survives reload), and the mobile drawer focus trap (Tab/Shift+Tab wrap, Escape returns focus to the trigger).
- `e2e/skills.spec.ts` — Playwright e2e for the per-session skill picker (mocked /api/chat): dropdown lists 8 skills, toggling customizes the session and sends the narrowed `enabledSkills` in the request, "Use all" resets to defaults, and the override survives a reload via `getSessionSkills`.
- `e2e/rate-limit.spec.ts` — the one spec that hits the **real** /api/chat (no mock): POSTs until the server-side rate cap trips and asserts the 429 carries a valid `Retry-After` (1–60s) plus the standard error body. Runs only on the chromium project. With `AUTH_DISABLED` the bucket falls back to per-IP (same guardRoute path/contract as a signed-in user); it also guards the env-var `defaultLimit` regression (unset `CHAT_RATE_LIMIT` must still cap at 120).
- `e2e/clear-race.spec.ts` — regression test for the slow-save New Chat race: delays the `saveChatMessages` server action (matched by its `next-action` POST with a `"branches"` payload) via `page.route`, clicks New Chat while the first save is still in flight (`sessionId` still null), and asserts the thread clears immediately **and** stays cleared once the save resolves — a stale `onSessionChange(sid)` publish must not resurrect the conversation. Runs on both projects. (Verified: reverting the `genAtSave` guard makes this spec fail.)
- `e2e/webhook-rate-limit.spec.ts` — route-level webhook guard tests (real /api/webhooks/stripe, chromium only): a single validly-signed event is acknowledged (200 `{ received: true }`) proving the guard isn't a block, then a flood of ~600 requests trips the 600/min cap with 429 + `Retry-After`. The valid test signs with `whsec_e2e_testing` — the same `STRIPE_WEBHOOK_SECRET` the CI workflow writes to .env — and skips when the server answers 501 (secret unconfigured). The two tests share the webhook bucket, so they run in declaration order (valid first, flood second).
- `e2e/visual.spec.ts` — visual regression snapshots (baselines in `e2e/visual.spec.ts-snapshots/`; regenerate with `--update-snapshots` after intentional UI changes). Tests force `prefers-reduced-motion` for stable snapshots. Covers the empty state, a mocked stream thread, the open mobile drawer (mobile project only — the DB-dependent session list is masked so the baseline stays deterministic), and the 404 + error-boundary pages in both themes (the latter via the test-only `app/error-demo` route, which renders the real `app/error.tsx` boundary with mock props).
- `tests/skill-utils.ts` — shared helper: a minimal frontmatter parser for the simple `key: value` frontmatter skills use (swap for js-yaml if skills ever need richer YAML).

Note: the community-installed skills (via `npx skills add`) — `react-best-practices`, `vercel-react-best-practices`, `zod-schema-validation`, `prisma-database-setup`, `prisma-cli`, `prisma-client-api`, `server-actions`, `nextjs-server-actions`, `prompt-optimizer`, `arize-prompt-optimization`, `compression`, `session-compression`, `accessibility`, `assistant-ui`, `loading-states-and-perceived-performance`, `micro-interaction-spec` — have their bundled rule files treated as vendor content and excluded from Prettier (see `.prettierignore`).

Adding a convention that must hold for all agents or skills means adding a case to the relevant suite — keep `npm run check` green.## Conventions

- Formatting/linting: `npm run lint` = `eslint .` (Next core-web-vitals + TypeScript rules) then `prettier --check .` then `tsc --noEmit` (strict); `npm run format` rewrites with Prettier (config in `.prettierrc.json`, style: no semicolons, single quotes, trailing commas, 100 cols)
- Vendor `.agents/types/**` are upstream Codebuff definitions — `no-explicit-any` / `no-empty-object-type` are disabled for them in `eslint.config.mjs`
- TypeScript is pinned to ^6 — typescript-eslint does not support TS 7 yet; keep it in sync if that changes
- Patterns to follow:
  - Agent file name must match id: `.agents/<id>.ts`, id = `^[a-z0-9-]+$`, unique across repo
  - `displayName` and `model` required; import types from `./types/agent-definition`
  - Prefer `instructionsPrompt` over `stepPrompt`/`systemPrompt`
  - MCP secrets via `'$VAR_NAME'` env syntax, not hardcoded
  - `outputSchema` only with `outputMode: 'structured_output'`
  - App secrets only via server env; LLM credentials never in client code
- Things to avoid:
  - Agents that modify files unless the agent's purpose is editing (e.g. the code reviewer is intentionally read-only)
  - New conventions that must hold for all agents — encode them in `tests/agent-definitions.test.ts` and keep `npm run check` green

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
